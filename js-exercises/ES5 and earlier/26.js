const  quote = 'With great power comes great responsibility';

const newQuote = quote.replace ( 'responsibility', 'electricity bill' );
console.log ( newQuote );
console.log ( quote.indexOf( 'great' ) );
console.log ( quote.substr ( 0, 10 ) );